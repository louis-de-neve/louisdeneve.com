version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Deploying static website with Substack integration"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
    exclude:
      - .git/**/*
      - README.md