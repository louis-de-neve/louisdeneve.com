version: 1
frontend:
  phases:
    build:
      commands:
        - node scripts/generate-manifest.js
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
    exclude:
      - .git/**/*
      - README.md