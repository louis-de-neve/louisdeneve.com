{
  "name": "louisdeneve.com",
  "version": "1.0.0",
  "private": true,
  "description": "Personal website of Louis De Neve",
  "scripts": {
    "generate-manifest": "node scripts/generate-manifest.js",
    "generate-crops": "node scripts/generate-crops.js",
    "prebuild": "npm run generate-manifest && npm run generate-crops",
    "build": "npm run prebuild"
  },
  "devDependencies": {
    "sharp": "^0.34.0"
  }
}